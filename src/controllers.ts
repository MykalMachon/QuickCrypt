export const createPassword = () => {};

export const listAllPasswords = () => {};

export const getPasswordByKey = (key: string) => {};

export const editPasswordByKey = (key: string) => {
  // TODO: edit a password by its key
};

export const deletePasswordByKey = (key: string) => {
  // TODO: delete a password
};
