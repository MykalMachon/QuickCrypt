// this is where we do all the fancy inquirer stuff

const createPasswordFlow = () => {
  // TODO: move inquirer / user flow actions into this method
};

const listAllPasswordsFlow = () => {
  // TODO: move inquirer / user flow actions into this method
};

const getPasswordByKeyFlow = () => {
  // TODO: move inquirer / user flow actions into this method
};

const isAuthenticatedFlow = () => {
  // TODO: move inquirer / user flow actions into this method
};
